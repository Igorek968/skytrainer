/** Поиск и оформление заказа на /client доступны гостям; «Мои заказы» — в client/orders/layout. */
export default function ClientAreaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
