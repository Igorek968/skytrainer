# Коррекция аудита Метрики (14.08.2026)

Счётчик `111110026` «ТвойТренер начало», период 8–14 авг 2026.

## Исправленная картина

Раньше `*_submit` стреляли на каждый клик. Реально за неделю: ~3 успешные анкеты инструктора, ~4 клиента, 0 оплат.
В Директе ON только найм инструкторов.

## Сделано

### Метрика (API)
- Создана цель `register_success` (id 597323260)
- Создана цель `order_create` (id 597323261)
- `register_submit` / `order_paid` уже были

### Код
- `*_submit` только после валидации + RU-email
- `register_success` при входе в gate после регистрации
- Анкета инструктора: понятная ошибка + скролл + частые причины
- Marketplace: CTA «найти тренера», регистрация не как отдельный главный путь
- Текст `/register` про заказ после подтверждения почты
- Checkout: цели при успешной регистрации в окне заказа
